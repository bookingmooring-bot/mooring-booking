{
  "buildCommand": "cd \"Mooring Booking\" && npm install && npm run build",
  "outputDirectory": "Mooring Booking/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
